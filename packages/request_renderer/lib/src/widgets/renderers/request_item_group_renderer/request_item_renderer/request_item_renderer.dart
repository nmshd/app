import 'package:flutter/widgets.dart';

class RequestItemRenderer extends StatelessWidget {
  const RequestItemRenderer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
