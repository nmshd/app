import 'package:flutter/material.dart';

class PostOfficeBoxAddressRenderer extends StatelessWidget {
  const PostOfficeBoxAddressRenderer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [],
    );
  }
}
