name: renderers
description: A new Flutter package project.
version: 0.0.1

environment:
  sdk: "^3.2.0"
  flutter: ">=3.16.0"

dependencies:
  enmeshed_types: 1.0.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
